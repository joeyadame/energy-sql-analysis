# Data Directory

Use this directory for local extract outputs such as CSV files generated by the
Python extraction scripts.

Example:

- `retail_sales_extract.csv`

Raw extract files are ignored by Git so the repository can stay lightweight and
focused on code, SQL, and documentation.
